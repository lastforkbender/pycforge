# Checkpoint E Gate Evidence

Status: promoted and sealed  
Distribution: PyCForge `0.14.4`  
Semantic boundary: sealed Phase 14D contracts at `0.14.3`

## Authenticated predecessor

Checkpoint E used the exact sealed Phase 14D source archive:

- filename: `pycforge_phase_14d_v0_14_3.tar.gz`;
- size: 1,282,543 bytes;
- SHA-256:
  `13228fe8e40c89335cf1bb6c44a2ebb94bc581e287873520b7c530984053c4f1`;
- root: `pycforge_phase_14d_v0_14_3`; and
- inventory: 573 members and 495 regular files.

Authentication precedes extraction. The equivalence runner extracts only
checked regular members into a private temporary directory and invokes the
predecessor with the current Python interpreter under `-I -S`. Transport is
canonical JSON only. No pickle, native toolchain, or generated-C execution is
used.

## Executable feature-matrix gate

The byte-exact semantic matrix remains SHA-256
`ca78dff3ea203130781f5e0fde879c0ca9d7b7a0e550a05ab5d46ea3432cc01a`,
schema `pycforge.feature-matrix/0.14.3`, and 69 ordered entries.

Every exact `(construct, context)` row has one explicit public-converter
witness:

- 37 supported entries convert and pass independent generated-C text
  conformance;
- 31 unsupported entries reject, publish no C, and carry their recorded
  diagnostics;
- the deferred `Match` entry rejects at its stable default boundary; and
- one unlisted `Try` witness proves the default unsupported policy.

The gate executes 70 primary witnesses plus one diagnostic-precedence profile.
It records 37 `Converted` and 33 `Rejected` primary results, zero contract
mismatches, and zero witness errors. `AsyncFunctionDef` proves its matrix-owned
Phase 9 `PYC2902` diagnostic and separately records current-default `PYC3509`
precedence.

Evidence digests:

- witness manifest:
  `b0b763058399edca6482ba7ab93c791f8b93c840b46f32a7cd0abe7813ba8b18`;
- execution:
  `66527ec9e7eff8debe0d5ba9f36738e349c684ce544f2339608ecf52bd9cd78f`;
- matrix report:
  `e4a2ddf3e202ee4630f7a0c217afce8c3fb63f297fa48873af1a664b11eecc85`.

## Full-supported-subset hardening

`pycforge.laboratory.checkpoint_e` drives only the public
`PythonToCConverter`. Its fixed corpus has 16 positive witnesses, one for each
promoted construct family. The seeded generated corpus has 64 cases: four
cases for every one of the same 16 families.

The generated set now explicitly includes:

- eight fixed-container-iteration cases;
- four floating-division cases; and
- complete seeded breadth across literals, arithmetic, calls, control flow,
  containers, explicit bundles, records, floor arithmetic, conditional
  regions, direct keywords, and required keyword-only calls.

For each of the 80 cases, the gate performs two full-observation conversions,
one observer-disabled conversion, and leading-comment, CRLF, and blank-line
metamorphic conversions. This is 480 public-facade conversions, including 240
metamorphic conversions. Every accepted output passes independent C-source
text conformance.

Evidence digests:

- promotion corpus:
  `b42f01c16cdeb466c9f690ce67be1bee3fe20f136f87b05746ba240d2b1701d5`;
- full-subset report:
  `2fa7089e7b8bbec1c89a88dcabffdd029e2dbd5ff5cff10e4ed42d883e0a7594`.

## Exact predecessor equivalence

The same 16 fixed and 64 generated promotion cases execute against the
authenticated 0.14.3 tree. Exact `result_to_json` bytes match for 80 of 80
cases, with zero missing, unexpected, or mismatched results. Candidate and
predecessor aggregate result digests are both:

`e4ad127d681523c71fdb3fad15764bc657a88eeb77a7c45a869c6f2a69e21b86`.

The isolated equivalence-corpus digest is
`b9792235cd9f802e37fddbf9f94a44fd0ac4822baa43f134f34a9dc219fb41f5`.

## Architecture and product gate

All cumulative source-only architecture audits pass. The central lowerer
remains at its existing 1,000-line ceiling and no ownership inversion is
introduced.

Checkpoint E additionally proves:

- package `0.14.4` is separate from converter contract `0.14.3`;
- every fact, plan, C IR, generated-C, summary, trace, rule-set, renderer, and
  policy identity remains the sealed Phase 14D value;
- converter and IDE runtime modules introduce no subprocess, foreign-function,
  dynamic-execution, compiler, linker, loader, or generated-code execution
  path;
- the public scripts remain `pycforge` and `pycforge-workspace`;
- no run, build, debug, terminal, plugin, or project-explorer action is added;
  and
- the active product identity is PyCForge.

Three non-exported legacy theme aliases remain only for sealed historical
validator custody. Current UI code does not import them.

## Workspace identity and migration

The active desktop contract is `pycforge-workspace/0.2`. Generated C remains
read-only and freshness-gated; closed `SourceBundle`, dirty-close, and atomic
save protections remain unchanged.

The active settings namespace is `PyCForge/PyCForge`. The one-time legacy
migration:

- accepts only a version-1 predecessor schema;
- migrates only bounded allow-listed presentation values;
- never overwrites an existing PyCForge value;
- never migrates source, generated C, artifacts, or credentials; and
- leaves the predecessor store unchanged.

An explicitly incompatible legacy schema migrates nothing. Twelve real-PyQt
tests are expected skips in this environment because PyQt5 is unavailable.
Checkpoint E makes no visible-widget or Windows 11 execution claim.

## Cumulative validation

The final suite discovers 589 tests: 572 pass, 17 are expected skips, and zero
fail. The skips are exactly 12 for unavailable PyQt5 and five for unavailable
older custody artifacts. The focused Checkpoint E test set passes 46 of 46.

The following source-only audits pass:

- architecture, rules, helpers, containers, modules, records;
- numeric, conditional, keyword, keyword-only, determinism; and
- Phase 14A, 14B, 14C, and 14D transitions.

The cumulative determinism SHA-256 remains
`6d132fa7544ea5d8b609689907c014b14c663cdd4999b47d921836182c38a35a`.

Promotion mode cannot pass with skipped fuzzing, fewer or more than 64
generated cases, missing matrix/default coverage, absent predecessor custody,
anything other than 80/80 exact equivalence, or incomplete final records.
Sealed mode additionally requires an assigned self-excluding release-tree
fingerprint. The canonical package builder rejects skipped isolated smokes,
partial promotion evidence, an incomplete 17-icon inventory, altered entry
points, native binaries, or a reused output directory.

## Non-claims and next boundary

Checkpoint E records, but does not implement or claim, the Phase 15
process-isolated worker, hard cancellation, maximum-input responsiveness,
virtualized projections, custom gradient/icon main and context menus, complete
IDE-grade feature set, visible accessibility gate, Windows 11 gate, or visible
Linux gate.

Checkpoint E opens neither Phase 14E nor Phase 15. The next separately
authorized work is Phase 15A responsiveness and process isolation under
Architecture Revision 3.3.
