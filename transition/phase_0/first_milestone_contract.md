# Approved First Milestone Contract

Input is exactly the fixture `fixtures/first_milestone/add.py` under Python 3.11 grammar and strict built-in annotation trust.

The expected immutable RulePlans, representation, absence of helpers/approximations, C source shape, and non-execution boundary are recorded in adjacent fixtures and specifications.

Approval criteria for later Phase 6:

- API and CLI use the same facade and have no PyQt dependency
- output and decision evidence are deterministic across fresh processes
- every selected rule and obligation is inspectable
- no partial C is published on rejection, failure, cancellation, observer failure, or resource exhaustion
- generated C is structurally validated but never compiled or executed
