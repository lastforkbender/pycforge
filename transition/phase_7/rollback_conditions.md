# Phase 7 Rollback Conditions

Rollback to the untouched Phase 6 archive if workspace implementation requires changing supported syntax, rule selection, RulePlans, semantic obligations, representations, lowering, C IR, rendering, generated-C conformance, or any execution/toolchain behavior.
