# Phase 8 Rollback Conditions

Reject the candidate and preserve v0.7.0 unchanged if any control-flow form bypasses C IR, changes output under observer settings, evaluates a chained operand more than once, permits stale GUI publication, weakens the no-execution boundary, silently approximates dynamic truthiness or iteration, or fails any earlier phase suite.
